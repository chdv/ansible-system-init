#!/bin/bash

#find . -type d -execdir flac_tag \;

for dir in */; do
  if [ -d "$dir" ]; then # Ensure it's a directory
    (cd "$dir" && flac_tag "$@") # Execute in a subshell to return to parent directory
  fi
done